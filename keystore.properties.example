# Copy to a path OUTSIDE the git repo (e.g. /home/box/secrets/tanniscoring/keystore.properties)
# and point TANNISCORING_KEYSTORE_PROPERTIES at it, or use that absolute path as default.
# NEVER commit real keystore.properties or *.keystore files.
storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=upload
storeFile=/home/box/secrets/tanniscoring/upload.keystore
